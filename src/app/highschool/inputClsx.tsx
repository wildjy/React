import clsx from 'clsx';

// const Input = ({ baseTy, ghost, disabled, readonly }) => {
//   const inputClass = clsx('px-3 py-2 border-gray-900', {
//     'border': baseTy,
//     'border-b': ghost,
//     'bg-baseGray': disabled,
//   })

//   return <input type="text" value="" className={inputClass} disabled={disabled} readonly={readonly} />
// }

// export default Input;