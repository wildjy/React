import { Meta, StoryObj } from "@storybook/react";
import HighSchoolPage from "./page";

export default {
  title: "Pages/HighSchool",
  component: HighSchoolPage,
} as Meta;

type Story = StoryObj;

export const Default: Story = {
  args: {},
};
